[skill]
name = "browser"
version = "1.0.0"
description = "Chrome CDP browser automation tools"
group = "browser"

[skill.requires]
python = ["websocket-client"]
system = ["Chrome with remote-debugging enabled (--remote-debugging-port=9222)"]
