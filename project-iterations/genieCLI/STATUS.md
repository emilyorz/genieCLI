# genieCLI — Workflow Status

> Living dashboard. First read on every session.
> Migrated to task-ledger-cycle v2 spec on 2026-04-17. v25 closed same day.

## Active Iteration

- **Ledger:** [CURRENT.md](CURRENT.md) holds **v31** (last _formal_ strict-V3 iteration, DONE). v32 + v33 + v34 + v35 + v37 shipped as out-of-process / TLV4-local iterations — see archives.
- **Status:** v31 (formal) + v32 + … + v39 + v40 + **v41** complete. v37–v41 all on `main` and **pushed to `origin/main`** (local 0/0 synced). Authoritative full suite: **1142 pass, 11 skipped**.
- **v40 (per-fragment decompose into write-advisory, with column-safety gate):** Sam wanted complex CTAS to be "拆開→逐塊分析→assemble", not handed to the LLM whole. Wires the v37 `decompose → optimize → recompose` pipeline into `run_write_analysis_only` in **advisory, non-executing** mode (`_run_decompose_advisory`): splits the inner SELECT into fragments, optimizes each via LLM, recomposes one advisory SQL; `verify()`/`baseline()` never called, only `provider.complete_text`. **Run via task-ledger-v4 form-2 (feature profile)** — discussion/explore/prototype passed, but spec stalled (opus producer thrashed on 19 panel issues across 2 harness stalls, ~8.5M token). An independent opus gate review caught a **real defect the panel and I nearly let pass**: the spec's safety argument (I9 "rules never change projection columns") was a false premise — R1-R10 are detectors only; the actual rewrite is an _unconstrained_ LLM (`trino_optimize.py:738`), and R2 literally suggests expanding `SELECT *`, so an LLM can change a fragment's columns; with `recompose` substituting CTE bodies, the outer arity guard misses it → semantically-wrong CTAS. Sam chose option A → main-thread took over implementation. **Fix = `_column_safe_candidates`**: compares output column set (`query_output_columns`) before/after each admitted+changed candidate and reverts (drops) any that changed or is indeterminable (`SELECT *`). New helpers `rewrap_ctas_inner_select` + `query_output_columns` (`sql_extraction.py`). Report gains `## Decomposed Optimization (advisory, per-fragment)` (per-fragment table + `Recompose verdict:` from RecomposeStatus + optional `### Recomposed advisory SQL (UNVERIFIED)` + reverted line); closed degrade taxonomy (no_provider/degraded_setup/degraded_pipeline). v35 safety contract unchanged. 19 new tests (`test_write_analysis_decompose.py`, incl. the column-gate revert proof) + 3 existing write tests relaxed from `assert_called_once` to `assert_called` (decompose adds LLM calls). End-to-end verified: unsafe column-adding rewrite reverted, safe same-column rewrite preserved. **Post-commit ledger-Verify review** (3 independent reviewers — correctness/adversarial/safety — on the real diff `29ae8e9`): all PASS, no hard_fails (8/7/9), each ran repros + patched verify/baseline to prove the non-executing invariant. The adversarial reviewer surfaced a genuine **scope-honesty gap**: `_column_safe_candidates` guards column NAMES only, not row-level semantics — an LLM rewrite that keeps column names but drops a `WHERE`/changes a JOIN (R6/R9 are rewrite-tier and DO touch predicates) passes the gate. Fixed by honesty, not overclaim: verdict copy changed from "no new blocking issue" → "checks structure/column shape only, NOT row-level semantics; verify equivalence"; docstring scope-limited; qualified-star (`t.*`) indeterminable bug fixed (correctness reviewer). +3 tests, **1097 pass, 11 skipped**. **Deferred (v41):** full AST-semantic equivalence gate for fragment rewrites; tGenie P1–P8 docs (P5 gap); `.tlv4-v40-decompose-write/` ledger left uncommitted (form-2 run was stall-interrupted + heavily hand-driven — not clean evidence).
- **v39 (R10 `join-key-computed` static rule):** Sam's v38 CTAS result put a computed value on a join key and nothing flagged it. Investigation (corrected Sam's recollection) found the "no compute in join key" rule was **never implemented** anywhere — R1 reads a join's `on` but only checks it exists, R9 skips consumers that have joins, R6 is WHERE-only, SKILL.md mentioned sargability only for WHERE, and tGenie P1–P8 never codified it (P5 undefined). New rule **R10 `join-key-computed`** (`rules/r10_join_key_computed.py`) walks each `exp.Join.on` for `exp.EQ` where one side wraps a column in a function/cast/arithmetic and the other is a bare column → `medium`; symmetric normalization (`LOWER(a.x)=LOWER(b.x)`) → `low`. Bare-column joins, range/inequality joins, and WHERE-side functions never fire. Wired through all five registration points (rule_ids `ALL_RULE_IDS` + `_load_rules` + `_STATIC_ACTIONS` ADVISE/medium + `_RULE_KIND_MAP` `fix-join-key-computed`) — guarded by `test_rule_id_contract.py`'s exact-coverage assertion against the v32 silent-fall-through bug. Also added to SKILL.md Anti-patterns so the LLM learns it. Because R10 runs in `static_analyze`, it flows automatically into the v38 write/CTAS directed-diagnosis path. 15 new tests (`test_r10_join_key_computed.py`) + corpus/contract updates. Full suite **1075 pass, 11 skipped**. **Deferred to v40:** wiring v37 `trino_optimize` decompose/recompose into write-advisory mode (fragment-by-fragment optimize for complex multi-CTE CTAS); tGenie P1–P8 documentation (P5 gap).
- **v38 (write/no-table non-executing optimization, uncommitted→`main`):** Side-effecting SQL (CTAS, INSERT, etc.) routed to the v35 write-analysis path used to degrade to `static_analyze` + one LLM call — it skipped every other zero-cost optimization step the read-only `--diagnose-only` path runs. Sam: "就算不能跑 write 或 table 不存在,也要走過精心設計的優化步驟(非執行的)". Fix wires the SAME non-executing directed-diagnosis pipeline into `run_write_analysis_only`: `pre_execution_diagnosis` (R1–R9 static + SQL-shape CTE/raw-scan contributors; live inputs `explain_cost`/`table_metadata`/`peak_memory` all `None` → degrade to empty, **zero cluster touched**) + `build_rule_gate_summary` action-tier classification. CTAS gets its inner SELECT extracted (`extract_ctas_inner_select`, sqlglot, falls back to whole statement if unparseable — e.g. reserved-word table names) so directions target the query, not the DDL shell. Report gains `## Ranked Optimization Directions` + a real `## Rule Gate` (counts replace the old hard-coded string); the advisory LLM prompt is fed the ranked directions + rule-gate so the rewrite follows them. v35 safety contract unchanged (SQL executed=no / EXPLAIN=no / Verified=no). Earlier same-session hotfixes (`7f1a72e` read-only no-data spinner+optimize; `6e753d9` write-analysis spinner + guaranteed Suggested SQL) preceded this. 11 new tests (`test_ctas_inner_select.py` + CTAS/non-CTAS/None-input/safety in `test_run_loop_mode_dispatch.py`); end-to-end verified with a real HumanSink CTAS (3 sections + spinner). **Follow-ups:** v39 added R10 join-key rule (below); decompose-into-write deferred to v40.
- **v37 (TLV4 third profile — `trino-optimize`, on `main` `93efc0b` merge + `96fb18f`):** Sam's manager's 4h→1min decompose/optimize/recompose method built as a Task-Ledger-V4 third profile, split into Run A (deterministic primitives) + Run B (pipeline) + live golden-case fixes. **Run A:** `detection_scan.py` (3-stage detection: pre-split / post-split-per-fragment / post-recompose), `cost_reader.py`, `rows_equivalent.py` (row-equivalence P0 gate). **Run B:** `trino_optimize.py` (~1018 lines) — 5-stage `baseline/decompose/optimize/recompose/verify` pipeline + sweep-by-monster driver, verdicts SHIP / NO_SHIP / UNVERIFIED / NO_COST_IMPROVEMENT. **Live golden-case fixes (`0bec7ca`):** `_apply_rewrites` whole-query `__root__` apply + recompose clean-rescan UNCERTAIN→PROCEED handling, found by `golden_case_run.py` against a real local Trino. **Static pack:** sqlglot R9 `join-first-filter-late` conservative rewrite candidate. **`96fb18f`:** no-data path now surfaces the optimized SQL + live spinner. Design docs `edfa70a` (master plan + open gaps) + `9a3b16e` (Option C taxonomy + 3-stage detection + FP/state-machine split). **No `archive/v37.md` retro file yet.** Open gap recorded in design: tGenie P1–P8 full definitions still undocumented (P5 missing). Touched feature: [trino-research](features/trino-research.md) (R9 rule pack + write-analysis report surface both tagged v37).
- **v36 (TLV4 Codex bugfix profile, on `main` `fc95f9b`):** repo-local cleanup of v34 memory-limit residuals. Fixed stale `make_query_max_run_time_sql()` docstring, made T-F-24 executable for MCP plan-cost memory-limit threading, and clarified bad env + failed `SHOW SESSION` breadcrumb to state the 1.0 GiB fallback is active. No live office `mcp-trino` / `localhost:8811` / `SHOW SESSION` numeric validation was performed; Sam marked item 1 unavailable for this run, so that remains residual. External `validate_ledger.py` v3 schema support remains out-of-repo. Full suite at v36 **935 pass, 1 warning**. Archive: [archive/v36.md](archive/v36.md). V4 evidence: `.tlv4-v36-cleanup/`.
- **v35 (TLV4 Codex full feature profile, DONE):** `/trino-research` write-operation analysis-only mode. Side-effecting SQL in `--file` mode is classified before MCP config/client/list_tools and routed to offline advisory analysis; direct guard runs after SQL acquisition; MCP entrypoint `sql_file`/`sql_text` guard also runs before MCP config/client/list_tools. Covers DML, CTAS, `EXPLAIN` wrapped writes, DDL including explicit `RENAME`/`REVOKE`, `CALL`, transactions, and unsafe multi-statement writes. Read-only MCP strictness preserved. Attempt 2 moved classifier/report helpers into import-safe `genie/skills/mcp_trino/write_analysis.py`; attempt 3 removed the chat-startup live MCP probe so `/trino-research --file <write.sql>` can classify before any MCP config/client/tool call. Hotfix: Ctrl-D interactive paste now exits directly and MCP read-only paste prompts metric/iterations/runs before preflight/EXPLAIN. Full suite **934 pass, 1 warning**. Archive: [archive/v35.md](archive/v35.md).
- **v34 (Task-Ledger-V4 pilot, `8ecced9`):** Memory-pressure threshold live wiring — per-node limit resolved from env / SHOW SESSION / 1 GiB fallback; `GENIE_TRINO_MEMORY_LIMIT_PER_NODE_BYTES` + `GENIE_TRINO_MEMORY_PRESSURE_FRACTION` env knobs; five call sites threaded; 53 new tests; **880 pass, 10 skipped, 0 failures**. Two back-edges (explore→attempt-2 caught total-vs-per-node inversion; review→develop caught nan ValueError). Full retro: [archive/v34.md](archive/v34.md). **4 residuals surfaced — see below.**
  - **v34 bugfix (TLV4 bugfix-profile pilot, `f0550b8`):** Fixed v33-residual-#3 — plan-cost zero-collapse on bytes-only partial EXPLAIN (`(rows or 0)*(bytes or 1)` → `_combine_cost`). `_combine_cost` added to `preflight.py`; all 4 call sites patched symmetrically across both MCP and `--direct` paths; baseline-None TypeError (OI-01) guarded. 15 new tests; **895 pass, 0 regression**. 3 followups carried to v35: cross-dimension partial comparison spurious measurement (minor, new), plan-cost loop metric-improvement gate (P2, pre-existing), `_combine_cost(0,None)→0` missing test case (nit). Retro: `.tlv4-pilot-bugfix/artifacts/wrap_retro.producer.md`.
- **v33 (out-of-process, orchestrated):** MCP long-query plan-cost parity (T1, quality-verified 9.3) + dual-path real-rule_id equivalence test (T2) + memory-pressure threshold calibration **mechanism** (T3 — wired to a live limit in v34). **819 pass.** Full retro: archive/v33.md.
- **v32 (on `main` `12e954a`):** rule_id contract repair (revived v31's never-firing `REWRITE` class) + per-iteration re-diagnosis + observational direction efficacy. archive/v32.md.
- **Touched features:** [trino-research](features/trino-research.md)
- **Last completed iteration:** v40 — per-fragment decompose→optimize→recompose into write-advisory, with a column-safety gate (delivers Sam's "拆開→逐塊→assemble").
- **Resume action:** v39 committed on `main`, push pending Sam's say-so. Next planning pass: write missing `archive/v37.md`–`v39.md` retros; **v40 candidate = wire v37 `trino_optimize` decompose/recompose into write-advisory mode** (fragment-by-fragment optimize for complex CTAS — Sam's "拆開→逐一分析→assemble" request); document tGenie P1–P8 (P5 gap). `CURRENT.md` still reads v31 (v32–v39 were out-of-process / TLV4-local records).

## Backlog / Carryover (after v36 cleanup)

From v34 retro (see [archive/v34.md](archive/v34.md)). v35 was redirected to the `/trino-research` write-analysis ticket. v36 consumed the repo-local minor residuals 4-6; these remain backlog/carryover candidates for the next planning pass:

1. ⭐ P1 — **Live-cluster numeric validation (#1):** verify whether Sam's office mcp-trino (`SHOW SESSION` on port 8811) exposes `query_max_memory_per_node`, its real string format/casing, and whether `0.5 × limit` is the right production fraction. Env override is the production path until validated.
2. ⭐ P1 — **Strengthen + clean (carried from v33):** pin expected kinds for all 8 rules in the dual-path equivalence test (T2 gap); remove the dead `mcp_explain_runner is not None` sub-condition; track/​fix the partial-EXPLAIN cost distortion `(rows or 0)*(bytes or 1)` (both paths) + extract the shared plan-cost iteration core before a 3rd copy.
3. ⭐ P1 — Upgrade `validate_ledger.py` to recognize the v3 ledger schema (carried v29→v34, still open). **Out-of-repo residual:** target Task Ledger tooling templates, not genieCLI product code.

Closed by v36: stale timeout docstring, T-F-24 conditional assertion, and `bad_env_fallthrough` fallback breadcrumb.

(3 promotes at cap.)
**Process (carried):** strict-V3 telemetry bottlenecks ad-hoc iterations on BOTH runtimes — reserve full strict V3 for codex-runner iterations; ad-hoc work = hooks-off + dispatched review + archive record. TLV4 flat SDD pilot (v34) ran to DONE without ceremony blockage — see archive/v34.md for pilot evidence. Backlog: EXPLAIN depth (join distribution/build-side); `--direct` permanent `table_metadata=None` asymmetry; LH-PRISM predictive cost engine.

## Active Parks

5 parks entering v30 PLAN (2 auto-dropped at v29 retro; 3 carried+aged + 2 new):

- E2E smoke mode labelling — age 2/3 — trigger: next time `kept=0/2` in an E2E report causes a human to suspect a product regression when it isn't — origin: v26-#change-next-1
- Cron plumbing (`E2E-REPORT.md outside repo` + `HTTP 401`) — age 2/3 — trigger: Sam actually tries to open an auto-generated E2E PR and finds the branch exists but no PR — origin: v26-#change-next-2
- Telegram allowlist plumbing — age 1/3 — trigger: next time Telegram reply errors AND the message was a status-flip ack — origin: v27-#failed-2
- Explain-runner closures untested — age 0/3 — trigger: a row-shape change in either explain runner (`_build_mcp_explain_runner` / `_direct_explain_runner`) ships a regression mocked tests miss — origin: v29-#failed-2
- Symmetry test can't compare explain-sourced axis — age 0/3 — trigger: Sam runs from a live cluster and a cross-path explain-direction divergence appears — origin: v29-#failed-3

(Auto-dropped at v29 retro: "Ledger roll-over drag" + "Autoresearch product-value signal" — both aged 3/3 without trigger (v29 closed same-day; no E2E decision in 4 iterations). See `archive/v29.md` Park aging pass.)

(Auto-dropped at v27 retro: "Display rounding hides sub-ms metrics", "`debug-mcp-tools.py` permanent home", "Always probe before patching MCP-contract assumptions". See `archive/v27.md`.)

(Auto-dropped in v26 aging pass: "Per-connector SKILL.md toggle" and "More connector-specific rules (Hive/Iceberg/Delta)". See `archive/v26.md`.)

## Theme Tracker

| Theme                                                                      | Appearances                                                        | Status                                                                |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------- |
| UX polish (cards / banners / spinners / syntax highlights / output layout) | v22, v23, v24, v26 (banner fast-fail), v27 (trino-research output) | long-term — no per-instance lifecycle; v27 is a substantial new entry |
| E2E signal hygiene (what the test measures vs what the output looks like)  | v27 parks (deferred)                                               | dormant — activates when a park trigger fires                         |

## Feature Index

| Feature        | Doc                                                      | Last touched                                                                                                                                                              |
| -------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| trino-research | [features/trino-research.md](features/trino-research.md) | v40 (per-fragment decompose→optimize→recompose into write-advisory; column-safety gate reverts column-changing LLM rewrites; `## Decomposed Optimization` report section) |
| mcp-banner     | [features/mcp-banner.md](features/mcp-banner.md)         | v26 (fast-fail 200ms probe; 14× cold-start improvement)                                                                                                                   |

## Archive

- **v40** (committed on `main`, push pending; **no `archive/v40.md` retro file yet**) — per-fragment decompose into write-advisory + column-safety gate. Wires v37 `decompose→optimize→recompose` into `run_write_analysis_only` (advisory, non-executing; no verify/baseline). Ran via task-ledger-v4 form-2 feature profile (discussion/explore/prototype passed; spec stalled on harness flake + over-thrashing panel, ~8.5M token; main-thread took over after an independent gate review caught a real false-premise safety defect). Core fix `_column_safe_candidates` reverts any LLM fragment rewrite that changes the output column set (the unconstrained-LLM hole). New `rewrap_ctas_inner_select` + `query_output_columns`. New `## Decomposed Optimization` report section. 19 new tests + 3 relaxed; 1094 pass / 11 skipped. Deferred: tGenie P1–P8 docs; `.tlv4-v40-decompose-write/` ledger uncommitted (interrupted form-2 run).
- **v39** (committed on `main`, push pending; **no `archive/v39.md` retro file yet**) — R10 `join-key-computed` static rule. Closes a real gap: no R1–R9 rule, prompt, or P-strategy ever inspected the JOIN `ON` predicate for a function/cast/arithmetic on a join key (the v38 CTAS case Sam hit). `rules/r10_join_key_computed.py` walks `exp.Join.on` → `exp.EQ` → computed-vs-bare side; medium severity, low for symmetric normalization; ADVISE action (no auto-rewrite). Wired through all 5 registration points (rule_ids/\_load_rules/\_STATIC_ACTIONS/\_RULE_KIND_MAP), guarded by the `test_rule_id_contract.py` exact-coverage assertion. Added to SKILL.md Anti-patterns. Flows automatically into the v38 write/CTAS directed diagnosis. 15 new tests; 1075 pass / 11 skipped. Deferred v40: v37 decompose into write-advisory; tGenie P1–P8 docs.
- **v38** (committed on `main`, push pending; **no `archive/v38.md` retro file yet**) — write/no-table non-executing optimization. CTAS / write SQL now runs the same zero-cost directed-diagnosis pipeline (`pre_execution_diagnosis` R1–R9 + SQL-shape + `build_rule_gate_summary`) the read-only `--diagnose-only` path uses, with all live-cluster inputs `None`. New `extract_ctas_inner_select` (sqlglot, graceful fallback) targets the CTAS inner SELECT. Report gains `## Ranked Optimization Directions` + real `## Rule Gate`; advisory LLM prompt fed the directions. v35 safety contract unchanged. 1060 pass / 11 skipped; 11 new tests + e2e HumanSink CTAS verify. Same-session hotfixes `7f1a72e` (read-only no-data spinner+optimize) + `6e753d9` (write spinner + guaranteed Suggested SQL) precede it. Deferred v39: v37 decompose into write-advisory; tGenie P1–P8 docs.
- **v37** (on `main` `93efc0b` merge + `96fb18f`; **no `archive/v37.md` retro file yet**) — `trino-optimize` third TLV4 profile: Sam's manager's 4h→1min decompose/optimize/recompose method. Run A deterministic primitives (`detection_scan.py` 3-stage detection / `cost_reader.py` / `rows_equivalent.py` P0 gate) + Run B `trino_optimize.py` 5-stage pipeline (baseline/decompose/optimize/recompose/verify) + sweep-by-monster + SHIP/NO_SHIP/UNVERIFIED/NO_COST_IMPROVEMENT verdicts; live golden-case fixes (`__root__` whole-query apply + clean-rescan UNCERTAIN handling) from `golden_case_run.py` against real local Trino; sqlglot R9 `join-first-filter-late`; 1047 pass / 11 skipped. Design docs `edfa70a` + `9a3b16e`. Open gap: tGenie P1–P8 undocumented (P5 missing).
- [archive/v36.md](archive/v36.md) — repo-local memory-limit cleanup for `/trino-research` (Task-Ledger-V4 Codex bugfix profile DONE): timeout docstring corrected for 2x/2000ms behavior, T-F-24 now executes MCP plan-cost memory-limit threading assertion, bad-env + failed `SHOW SESSION` breadcrumb says 1.0 GiB fallback is active; full suite **935 pass**, 1 warning; no live cluster probe; landed on `main` `fc95f9b`
- [archive/v35.md](archive/v35.md) — write-operation analysis-only mode for `/trino-research` (Task-Ledger-V4 Codex DONE): chat `--file` early write dispatch before MCP reachability, direct/MCP entrypoint guards, import-safe `write_analysis.py`, Ctrl-D paste hotfix, reports under `./report/trino-research-write-analysis-<ts>.md`; full suite **934 pass**
- [archive/v34.md](archive/v34.md) — memory-pressure threshold live wiring (Task-Ledger-V4 pilot, flat 9-step SDD): per-node limit from env / SHOW SESSION / 1 GiB fallback; `GENIE_TRINO_MEMORY_LIMIT_PER_NODE_BYTES` + `GENIE_TRINO_MEMORY_PRESSURE_FRACTION` env knobs; 5 call sites threaded; 53 new tests; **880 pass**, 0 regression; commit `8ecced9`; two back-edges caught real defects (explore: total-vs-per-node inversion; review: nan ValueError)
- [archive/v33.md](archive/v33.md) — directed-loop continuation (out-of-process, hooks-off + full dispatched review): MCP long-query plan-cost parity (T1, quality 9.3) + dual-path real-rule_id equivalence test (T2) + memory-threshold calibration mechanism (T3, not yet wired to a live limit); 819 pass; orchestrator=Claude, executor=local Codex CLI, verifiers=Claude sonnet/opus
- [archive/v32.md](archive/v32.md) — directed-loop repair (out-of-process hotfix, V3 deviation): rule_id contract fix (revived v31's never-firing rule-gate `REWRITE` class) + per-iteration re-diagnosis + observational direction efficacy, both paths; 809 pass; commit `12e954a` on main
- [archive/v31.md](archive/v31.md) — rule-first pre-AI gate (BLOCK/REWRITE/ADVISE/PASS) with compact TUI + shared MCP/direct/plan-cost behavior; 799 pass; commits `bc82bdf`/`21ca729`/`f274f10`
- [archive/v29.md](archive/v29.md) — directed pre-execution diagnosis (LH-PRISM) on both paths: ranked `OptimizationDirection` injected pre-iter-1 + zero-cost long-query report (`--diagnose-only`/gate-trip) + dual-path symmetry test; C2 hypothesis-structure closed; 781 pass 0 skip; built v3-strict (label `v3-deviation`, enforcement via dispatched spec+quality verifiers not live hooks); commits `006a2f9`/`62e4503`/`4acb7c4`
- [archive/v30.md](archive/v30.md) — long-query UX + Trino optimization input refresh: long-query default, elapsed stopwatch, candidate timeout, inline reject reasons, readable TUI blocks, SQL-shape diagnosis, README references; full suite 792 pass; commits through `c354b34`
- [archive/v28.md](archive/v28.md) — sqlglot AST 8-rule engine + plan_signature + plan-cost long-query loop + no-data dispatch; 724 pass +77 tests; T1/T7 skipped (live MCP probe → Sam env); MCP no-data dispatch hotfix `bd1a97a`
- [archive/v27.md](archive/v27.md) — `/trino-research` output UX overhaul (terminal slim + iteration-centric report + `./report/` subdir); single-commit T1-T4; hypothesis un-truncated; 641 tests pass; both v26 promotes deferred to parks at PLAN ack
- [archive/v26.md](archive/v26.md) — fast-fail MCP banner (T1 3000→211ms); smoke discipline note; v15 R3 + v10 T9 close-out; first post-hardthink-gate retro (process-gap + do-differently-next-time sections populated)
- [archive/v25.md](archive/v25.md) — metric pipeline fix (bare-list response handling + EXPLAIN ANALYZE backfill); pivoted from original async-banner focus
- All historical `TASK-LEDGER-v*.md` (v1-v24) under [archive/](archive/), naming kept for git-history clarity

## Meta-retro Log

- **First meta-retro DUE at v30** (5 iterations from v25 — the first under the new spec). v29 closed 2026-05-27; v30 PLAN must include the meta-retro pass.
- Full history: [LEARNINGS.md](LEARNINGS.md)

## Notes

- `MIGRATION-MAP.md` is pre-existing v8 skill-architecture migration record — left alone, not part of the new spec.
- `scripts/debug-mcp-tools.py` is a permanent diagnostic for MCP-contract issues. Three modes: default (list tools + show resolver choice), `--probe` (raw response shape per query), `--measure SQL` (full `_measure_mcp` chain dump). Used during v25 to find the bare-list response bug; kept for future MCP server compatibility checks.
- Pre-commit hook from workspace-emily lives at workspace-level only. To wire validation in genieCLI repo: copy `bin/validate-ledger-precommit.sh` from workspace-emily and install at `.git/hooks/pre-commit`. Until then, run `python3 ~/.claude/skills/task-ledger-cycle/templates/validate_ledger.py project-iterations/genieCLI` manually before committing ledger changes.
