# v38 — write/no-table non-executing optimization

> Backfilled 2026-06-13. Fuller detail: STATUS.md / features design log.

## Scope
Side-effecting SQL (CTAS/INSERT) and no-table queries should still traverse the full
zero-cost directed-diagnosis pipeline (non-executing), not degrade to one LLM call.
Sam: "就算不能跑 write 或 table 不存在,也要走過精心設計的優化步驟(非執行的)".

## What shipped
`run_write_analysis_only` now runs `pre_execution_diagnosis` (R1–R9 static + SQL-shape) +
`build_rule_gate_summary` with all live inputs `None` (zero cluster touched). CTAS inner
SELECT extracted (`extract_ctas_inner_select`). Report gains Ranked Directions + real Rule
Gate. v35 safety contract unchanged. Commit `95c2de6`.

## Verification
1060 pass / 11 skipped; 11 new tests + e2e HumanSink CTAS.

## Deferred
decompose-into-write → v40; tGenie P1–P8 docs (P5 gap) → eventually v47.
