# v44 — EXPLAIN-depth join diagnosis

> Backfilled 2026-06-13. Fuller detail: STATUS.md. (git: `c281c32`)

## Scope
Parse the already-fetched EXPLAIN plan for per-join distribution facts with zero extra
cluster round-trips — fold join-strategy signals into `pre_execution_diagnosis`.

## What shipped
`_join_diagnosis_contributor(explain_cost)` emits 3 signals: S1 broadcast-blowup (REPLICATED +
build > 1 GiB → high), S2 broadcast-candidate (PARTITIONED + small build → medium), S3
stats-gap (UNKNOWN distribution → low). Wired at all 4 call sites; fail-open on missing/malformed
plan. Named constants for the thresholds.

## Verification
+39 tests → 1275 pass / 1 skipped. Residuals need live-cluster validation of the encoding
field names (fail-open degrades to S3 on schema drift). Ledger `.tlv4-v44-explain-depth/`.
