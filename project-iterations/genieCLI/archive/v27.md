# Archive — v27 (read-only)

## Basic Info

- **Project:** genieCLI
- **Iteration:** 27
- **Status:** active (DO — PLAN v2 ack'd by Sam via Telegram msg 811)
- **Owner:** Emily (planning + execution); Sam picks direction
- **Started:** 2026-04-20
- **Updated:** 2026-04-20T21:55+0800
- **Focus:** `/trino-research` UX overhaul — terminal slim + iteration-centric report + ./report/ subdir. Removes three pain points Sam identified as "整個有點混亂".
- **Touched features:** [trino-research](features/trino-research.md)

## Goal

- **One-line summary:** Make `/trino-research` output scannable — no terminal dump of giant SQL, no cwd pollution, no triple-SQL duplication in the report.
- **Done when:**
  - Terminal after a run shows summary table + iteration history + `Report saved: ./report/<file>.md` — no full Optimized SQL printed to stdout.
  - Report has **one** Best SQL block (not three), with per-iteration sections that carry the full hypothesis (no 60-char truncation) + a mini diff scoped to that iteration's change + verdict + metric.
  - Report is saved under `./report/<name>.md` (auto-created), not directly in cwd.
  - `features/trino-research.md` reflects the new UX in Design log + Iteration touchpoint.
  - Sam sign-off on the new report/terminal layout (T5 dropped per msg 813 — no real-run verification in v27; will exercise in first natural use after merge).

## Carryover (from v26)

Max 3 items. v26 promoted 2 items; both **deferred** at v27 PLAN ack stage per Sam's Telegram msg 807 ("三個都還好") and msg 809 (picked D — UX — instead).

- ⭐ P0 S — E2E mode disambiguation — **deferred to park** (see Active Parks)
- ⭐ P1 S — Cron plumbing fix — **deferred to park** (see Active Parks)

## Promote Verification (mandatory first PLAN action)

Walk every Carryover item from v26. Outcomes filled at v27 PLAN ack stage (user-directed deferral, not waiting for Todo verification).

| From               | Item                    | Outcome                  | Evidence                                                                                                                                                                                |
| ------------------ | ----------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v26-#change-next-1 | E2E mode disambiguation | **deferred-by-user-ack** | Telegram msg 807 ("這三個都還好") + msg 809 (Sam picked D instead); moved to park with age 0/3 and revival trigger. Not lost — re-evaluatable at v28 retro or earlier if trigger fires. |
| v26-#change-next-2 | Cron plumbing fix       | **deferred-by-user-ack** | Same Telegram msgs; moved to park with age 0/3 and trigger "Sam actually tries to view an E2E PR and can't find it".                                                                    |

## Active Parks (carried from prior iterations + deferred v26 promotes)

7 parks entering v27 DO (5 carried + 2 deferred from v26 promotes).

- Display rounding hides sub-ms metrics — age 2/3 — trigger: real Trino query (not SELECT 1) shows misleading 0 AND user complains — origin: v25-#change-next-2
- `debug-mcp-tools.py` permanent home — age 2/3 — trigger: third time someone asks "how do I check if MCP integration is working" — origin: v25-#change-next-3
- "Always probe before patching MCP-contract assumptions" — age 2/3 — trigger: meta-retro at v30 — origin: v25-#failed-1
- Ledger roll-over drag — age 1/3 — trigger: a second iteration closes >1 day after final Todo accepted — origin: v26-#failed-2
- Autoresearch product-value signal — age 1/3 — trigger: v27's E2E mode decision lands AND smoke mode picked — origin: v26-#change-next-3
- **E2E smoke mode labelling (new)** — age 0/3 — trigger: next time `kept=0/2` in an E2E report causes a human (Sam, Emily, Elena, or onboarding) to suspect a product regression when it isn't — origin: v26-#change-next-1 (deferred at v27 ack)
- **Cron plumbing: `E2E-REPORT.md outside repo` + `HTTP 401` (new)** — age 0/3 — trigger: Sam actually tries to open an auto-generated E2E PR and finds the branch exists but no PR — origin: v26-#change-next-2 (deferred at v27 ack)

## Theme Tracker (cluster radar)

| Theme                                                                      | Appearances                                                        | Status                                                                |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------- |
| UX polish (cards / banners / spinners / syntax highlights / output layout) | v22, v23, v24, v26 (banner fast-fail), v27 (trino-research output) | long-term — no per-instance lifecycle; v27 is a substantial new entry |
| E2E signal hygiene (what the test measures vs what the output looks like)  | v27 parks (deferred)                                               | dormant — activates when a park trigger fires                         |

## Hardthink — Alternatives considered

### For T2 (Report iteration-centric rewrite)

1. **Full replacement** — delete Original SQL + Optimized SQL blocks entirely; the report becomes Summary card → per-iteration sections → final Best SQL (once). Diff lives inside each iteration section, scoped to just that round's change. Most aggressive; addresses "三份看三次" completely.
2. **Original as appendix** — keep Original SQL but move it to end of report under a `## Appendix` heading; Optimized SQL gone; per-iteration sections as in #1. Keeps reproducibility for someone who opens the report standalone but pushes it off the first screen.
3. **Minimal patch** — keep current layout but un-truncate hypothesis (drop `[:60]`) and add a verdict column. Cheapest, doesn't address Sam's "整個混亂" complaint.

**Recommendation: #1.** Sam explicitly named "三份看三次" as a pain. #2 adds structural complexity (appendix navigation) without solving it. #3 is rearranging deck chairs. If a consumer of the report needs Original SQL, they can read the first iteration's diff (delta from Original) or rerun with `--show-original` in a future iteration.

### For T3 (report path)

1. **`./report/`** — Sam's suggestion (msg 811 literally said "report folder"). Per-project, co-located with the SQL being researched. Clean under one project; noisy if Sam runs /trino-research from home or a temp dir.
2. **`~/.genie/reports/`** — global cache. Clean across projects, but decouples report from the project context (finding "the report for yesterday's PBB query" requires grep across all projects).
3. **`$XDG_STATE_HOME/genie/reports/`** — XDG-compliant version of #2.

**Recommendation: #1** (Sam explicitly picked it). If cwd-polution becomes annoying later, add `--report-dir` flag; don't pre-engineer.

### For T1 (terminal slim)

1. **Pure redirect** — terminal shows "Report saved: ./report/xxx.md", nothing else after the run (summary too). Saves the most scroll.
2. **Summary-only** — terminal keeps summary table + iteration one-liners (current lines 570-582 behavior), drops the full SQL print. Sam still sees the outcome at a glance without navigating to the file.
3. **Current + pagination** — pipe long SQL through `less` if tty, skip if not. Complexity, fragile.

**Recommendation: #2.** Sam's pain is the "70 lines of Optimized SQL洗版", not the summary/iteration lines — those are compact and useful. #1 is overcorrection (forces Sam to open the file even for quick runs). #3 overengineered.

## Hardthink — Scope

### In

- `genie/skills/trino_query/research.py` — three edits:
  - `run_trino_research` line 584-590: remove full Optimized SQL print; keep "Optimized SQL saved" pointer
  - `_generate_report` lines 390-462: rewrite to iteration-centric layout; remove Original/Optimized duplicates; un-truncate hypothesis; add per-iteration mini diff
  - Report save path lines 595-599: change `Path.cwd() / report_name` → `Path.cwd() / "report" / report_name` with `mkdir(parents=True, exist_ok=True)`
- `features/trino-research.md` — v27 Design log + Iteration touchpoint + Current capability bump
- `project-iterations/genieCLI/CURRENT.md` + `STATUS.md` — this PLAN v2 + Todo progression

### Out (explicitly deferred)

- Semantic diff (AST-level change summary) — larger body of work; can park if Sam wants it in v28
- Side-by-side / word-level diff rendering — current unified diff kept; no new dep
- `--report-dir` flag — add when someone asks; not pre-engineered
- Machine sink / JSON output format — untouched
- Interactive (`genie chat`) vs non-interactive (`--sql-file`) divergence — both behave identically after T1
- All seven Active Parks (aging-only, no implementation this round)

## Hardthink — Open questions

**None — proceeding.**

Rationale: Sam acked A/B/C via Telegram msgs 809 + 811 and explicitly dropped T5 in msg 813 ("T5 不用 , 先走 T1~T4"). Folder name fixed to `./report/` per his word. Diff style stays unified (no new dep). Interactive/non-interactive unified behavior (no question).

## Todo

| ID  | Status    | Pri | Task                                                                                                                                                                            | Feature        | Tool                                        | Verify                                                                                                                                                                                                     | Note                                                   |
| --- | --------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| T1  | completed | P0  | Terminal slim: drop full-Optimized-SQL print in `run_trino_research` (research.py:614-616); keep summary + iteration lines                                                      | trino-research | Edit + Read                                 | Re-read of changed lines confirms only "no improvement" fallback remains; 641 tests pass                                                                                                                   | Commit `e981d8f`                                       |
| T2  | completed | P0  | `_generate_report` iteration-centric rewrite: remove Original/Optimized full dumps; per-iteration section with full hypothesis + mini diff + verdict; one Best SQL block at end | trino-research | Edit + Bash (synthetic-data smoke + pytest) | Synthetic-history smoke produced expected layout (Summary → iteration table → per-iter sections w/ mini diff → single Best SQL); 641 tests pass; `_iteration_diff` helper uses stdlib difflib (no new dep) | Biggest code change of the iteration; commit `e981d8f` |
| T3  | completed | P0  | Report path → `./report/<name>.md` with `mkdir(parents=True, exist_ok=True)` (research.py:622-624)                                                                              | trino-research | Edit + Read                                 | Re-read of save block confirms `Path.cwd() / "report"` + `mkdir(parents=True, exist_ok=True)` in place; 641 tests pass                                                                                     | Sam's word: cwd底下開 report folder; commit `e981d8f`  |
| T4  | completed | P1  | `features/trino-research.md` — Design log entry (UX trio), Iteration touchpoint (v27), Current capability snapshot update                                                       | trino-research | Edit + Read                                 | Re-read confirms v27 entry under Design log + Iteration touchpoints + Current capability bumped; ledger validator clean                                                                                    | Commit `e981d8f`                                       |

## Reports

- T1 — terminal slim: research.py:614-616, 7-line block reduced to 3-line conditional fallback. No more "Optimized SQL:" header + 70-line dump. Verified by re-read of `run_trino_research`'s post-summary section.
- T2 — `_generate_report` rewrite: 73 lines → 80 lines (net +7), but layout completely restructured. Synthetic 3-iteration smoke (improved + semantic_drift + lint_failed) produced expected output: one Best SQL block at end, per-iteration mini-diffs, full hypothesis. New helpers: `_VERDICT` map (6 statuses → human verdict), `_iteration_diff` (stdlib difflib, n=2 context).
- T3 — report path: 1-line change `Path.cwd() / report_name` → `Path.cwd() / "report" / report_name` plus `mkdir(parents=True, exist_ok=True)`. No exception handling added (mkdir is idempotent; existing `try/except` around `write_text` already covers IO errors).
- T4 — feature doc: 2 edits — Current capability section (Post-loop UX + new Report UX bullet) and Design log v27 entry (full rationale + alternatives considered + supporting data change explanation).
- Supporting data change: `iter_base_sql` snapshot at loop top + `base_sql`/`candidate_sql` keys in all 5 history.append sites + un-truncate `hypothesis` at extraction (`line[:80]` → `line`; terminal display still truncates via `hypothesis[:80]`). Backward-compatible: report renderer uses `.get()` so older history dicts (if any persisted) still render.
- Tests: 641/641 pass on commit `e981d8f`. No test changes needed because no public-API surface changed.

## Blocked

- None — T5 dropped per Sam Telegram msg 813 ("T5 不用 , 先走 T1~T4"). v27 closes on T1-T4.

## Retro

### Worked

1. **Hardthink Alternatives table prevented over-engineering on T2/T3.** Documenting "full replacement vs appendix vs minimal patch" for the report rewrite forced an explicit choice instead of incrementally adding sections. Documenting "./report/ vs ~/.genie/reports/ vs XDG" let me defer the cross-project ergonomics question without implementing a flag for it. Net: zero scope creep on a UX iteration that historically grows.
2. **Reading code before proposing.** v27's PLAN named exact lines (research.py:584-590, :390-462, :595-599) because I read the file end-to-end first. Result: the "supporting data change" (need `base_sql` + `candidate_sql` per iteration to render scoped diffs) was caught at PLAN time, not mid-T2 implementation.
3. **User-driven re-plan handled cleanly.** v26 promotes were deferred at PLAN ack via Telegram msgs 807/809 — instead of mechanical Carryover→Todo, I recorded outcome `deferred-by-user-ack` in Promote Verification with msg-ID evidence and migrated both items to Active Parks with revival triggers. Items are not lost; their "second chance" is encoded as triggers, not as a backlog.
4. **Single commit for the UX trio.** T1+T2+T3+T4 went into one commit (`e981d8f`) because they're one logical UX overhaul. Splitting would have created an intermediate state where the terminal had been slimmed but the report still triplicate-dumped — strictly worse than either before or after. Conventional-commits + clear body kept the diff reviewable.

### Failed

1. **Validator's "Verify column required" rule wasn't checked at PLAN time.** I wrote the v27 Todo table without the `Verify` column; the warning only surfaced at the T4 commit because validator runs against staged ledger files. Cost: had to re-edit CURRENT.md after the work commit. — `drop — why: the actionable fix is captured in Change-next #1 (PLAN-time validator dry-run) with its own promote slot. Listing the missing-column slip here as a separately-promoted failed item would double-count the same cap. Process-level insight lives in the Process gap section.`
2. **Telegram ack to Sam (msg 813 acknowledgment) failed silently.** Tool returned `chat 1039910511 is not allowlisted`; I noted it and proceeded but didn't surface it as a blocker to Sam. If Sam is waiting on a "starting now" signal he won't get it. — `park age 0/3 — trigger: next time Telegram reply errors AND the message was a status-flip ack (e.g. "now doing X" / "T1 done"). Then either fix the allowlist mechanism or build an alternative-channel fallback. Reason for park (not promote): the chat-ID/allowlist plumbing is owned by the /telegram:access skill which I don't run; building a fix without a second occurrence risks pre-engineering, and Sam re-engaged the conversation directly so no information was lost in v27.`

### Change next

1. ⭐ P1 S — **PLAN-time validator dry-run.** Run `validate_ledger.py` against the freshly written PLAN before announcing "PLAN ack'd, entering DO" — catches missing-column / ack-gate / promote-cap issues before code work starts, not after. Cost: ~5s. Avoids the v27-#failed-1 pattern.
2. ⭐ P2 S — **Hypothesis prompt structure.** v27 un-truncated the hypothesis at storage (was `[:80]` → now full line), but the underlying extraction is still "first non-code line" — brittle when AIs bury hypotheses in code comments or after a heading. A structured `Hypothesis:` prefix in the AI prompt would make extraction deterministic. Already on the feature doc's Open Questions list; v27 made it more visible by un-truncating. Defer unless Sam asks — current heuristic works for qwen3.5:4b in observed runs.

(2 promotes — under cap of 3.)

### Duplicate check

- `validate-at-PLAN` not previously promoted. v23/v24 retros mention validator usage but as DO-time mechanic, not PLAN-time gate. Original promote.
- `Hypothesis prompt structure` is already in the feature doc's Open Questions (since v22+); v27 elevates it to a candidate promote because the un-truncation made the brittleness more visible. Status: pre-existing concern, **not** a duplicate promote — it's first-time advancement to ledger.

### Park aging pass

7 parks entering v27 → 4 parks carried + 3 auto-dropped + 1 new = 5 parks entering v28. All untriggered ages bumped +1; convention is parks reaching 3/3 in this round auto-drop in this same retro.

| Park                                        | Age in | Age out | Outcome       | Notes                                                                                                                                                                                                                           |
| ------------------------------------------- | ------ | ------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Display rounding hides sub-ms metrics       | 2/3    | 3/3     | **auto-drop** | No real Trino query produced misleading 0 in v25-v27. Trigger never fired.                                                                                                                                                      |
| `debug-mcp-tools.py` permanent home         | 2/3    | 3/3     | **auto-drop** | No third "how to check MCP" question in v25-v27. Script remains in `scripts/` per existing usage.                                                                                                                               |
| "Always probe before patching MCP-contract" | 2/3    | 3/3     | **auto-drop** | Original plan was "hold for meta-retro at v30". Reconsidered: meta-retro reviews all in-window retros without needing a parked-item placeholder — if the insight is real, v30 will surface it from the v25 retro text directly. |
| Ledger roll-over drag                       | 1/3    | 2/3     | carry         | v27 closed same-day as PLAN — counter-evidence; trigger needs a SECOND late-close instance, still 1 observed.                                                                                                                   |
| Autoresearch product-value signal           | 1/3    | 2/3     | carry         | E2E mode decision did not land in v27 (deferred); trigger still pending v28 or later.                                                                                                                                           |
| E2E smoke mode labelling                    | 0/3    | 1/3     | carry         | First aging since deferral; no `kept=0/2` confusion in v27 (no E2E run).                                                                                                                                                        |
| Cron plumbing (E2E-REPORT.md + 401)         | 0/3    | 1/3     | carry         | First aging since deferral; Sam did not try to open an E2E PR in v27.                                                                                                                                                           |

Add new park from v27-#failed-2: Telegram allowlist plumbing — age 0/3, trigger described in Failed-2 above.

Net: 7 in, 3 auto-drop, 4 carry, 1 new = 5 entering v28.

## Process gap

1. **PLAN-time validator dry-run is missing from the SKILL.md PLAN→DO transition checklist.** v27 made the same mistake v25 made: write PLAN, ack, work, then discover at commit time that the ledger has a structural issue (missing column, unhardthought open questions, etc.). The validator already exists; the gap is procedural — nothing in the PLAN ack ritual references it. Fix landing in `change-next-1` makes the implicit explicit.
2. **Telegram-as-ack-channel has a quiet-failure mode.** When the bot reply errors (allowlist, network, etc.), the agent currently logs locally and proceeds. For high-stakes acks (e.g. "starting work now", "T1 done") this is fine; for direction-change confirmations ("doing D, dropping E2E focus") the silent failure could cause Sam to assume one thing while Emily executes another. Not worth a process change yet (v27 was the first observed instance), but flagging for awareness.

## Do differently next time

1. **Validate the PLAN before announcing the ack gate is open.** Concretely: after writing CURRENT.md's PLAN section, run `python3 ~/.claude/skills/task-ledger-cycle/templates/validate_ledger.py project-iterations/<proj>` and resolve all WARN/ERROR before saying "ack'd, entering DO". 5-second cost, prevents post-hoc edits.
2. **For UX iterations specifically, build a tiny synthetic-data smoke harness BEFORE the main rewrite.** The `_generate_report` smoke in v27 was written _after_ the rewrite to verify the layout. If I'd built it first (with the target layout in the docstring), I could've TDD-style validated each section landing as expected. Would have caught the "no trailing newline → diff lines mash together" cosmetic issue earlier.
3. **When a Telegram reply errors out on an ack-relevant message, do NOT silently proceed — surface to the user via an alternative channel or hold work until the channel is restored.** Rule of thumb: if the message was a status flip ("now doing X"), it must reach the user. If the message was a polite "in progress", silent failure is fine.

### Next-round Focus (preview)

Top promotes from v27, with target features:

1. ⭐ P1 S — PLAN-time validator dry-run → process change (no feature doc; potentially a one-line addition to `~/.claude/skills/task-ledger-cycle/SKILL.md` PLAN section).
2. ⭐ P2 S — Hypothesis prompt structure → `features/trino-research.md` (extraction logic in `genie/skills/trino_query/research.py`).

(2 promotes — under cap of 3.)

## Roll-over Checklist

- [x] Promote Verification table filled (v26 deferrals recorded; v27 Todo items verified as they complete)
- [x] All Failed/Change-next items tagged
- [x] Promote count ≤ 3 (2 promotes — under cap)
- [x] `features/trino-research.md` updated with v27 Design log + Iteration touchpoint (T4)
- [x] Park aging applied (7 parks entering → 7 leaving + 1 new = 8; aging triggers checked, no auto-drops in v27 itself)
- [x] Theme Tracker — UX polish row bumped with v27 entry
- [ ] Move this file to `archive/v27.md` (next step)
- [ ] Create new `CURRENT.md` with Carryover from v27 promotes (next step)
- [ ] Update `STATUS.md`: Active Iteration pointer → v28, Next Iteration Focus, refreshed Parks, Feature Index (next step)
- [x] Run `scripts/validate_ledger.py` manually — clean after Verify-column fix
- [x] No meta-retro due (next at v30; v27 is round 3 of the post-spec run)

## Archive / Handoff

- When archived, this file becomes `archive/v27.md` (read-only).
- STATUS.md is the single entrypoint going forward.
