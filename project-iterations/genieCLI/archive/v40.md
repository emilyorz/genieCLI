# v40 — per-fragment decompose into write-advisory + column-safety gate

> Backfilled 2026-06-13. Fuller detail: STATUS.md (richest entry) / features design log.

## Scope
Wire the v37 decompose→optimize→recompose pipeline into `run_write_analysis_only` in
advisory, non-executing mode (no verify/baseline) so complex CTAS is split, optimized
per-fragment, and recomposed. Commit `29ae8e9` (+ `a3297d3` honest-scope fix).

## What shipped
`_run_decompose_advisory` + `_column_safe_candidates` (reverts any LLM fragment rewrite that
changes the output column set — closes the unconstrained-LLM hole). New `rewrap_ctas_inner_select`,
`query_output_columns`. New `## Decomposed Optimization` report section.

## Verification
1094–1097 pass / 11 skipped; 19 new tests incl. the column-gate revert proof. Post-commit
3-reviewer ledger-Verify (correctness/adversarial/safety) all PASS.

## Deferred / lessons
- **Key lesson:** an independent opus gate caught a false-premise safety claim the panel
  nearly passed (rules are detectors; the rewrite is an *unconstrained* LLM). Producer ≠ reviewer.
- The adversarial reviewer flagged that column-name safety ≠ row-level semantics → **full
  row-level semantic gate deferred to v41** (delivered there).
