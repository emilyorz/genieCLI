# v43 — plan-cost backlog bundle (TLV4)

> Backfilled 2026-06-13. Fuller detail: STATUS.md.

## Scope
Closed carried-P1 backlog from v33 — 3 scope items, no product behavior change.

## What shipped
- S1: extracted `_plan_cost_loop_core` / `_SafeOutput` / `_PlanCostCoreResult` into
  `preflight.py`; both MCP and `--direct` plan-cost loops call the shared core (no 3rd copy).
- S2: removed the dead `mcp_explain_runner is not None` guard.
- S3: pinned expected KIND + ACTION for every rule in `ALL_RULE_IDS` (R1–R10) — a new R11
  fails the dual-path equivalence test immediately.

## Verification
+29 tests → 1236 pass / 1 skipped. Dual-path symmetry preserved; behavior identical save one
cosmetic header change. Ledger `.tlv4-v43-plancost-core/`.
