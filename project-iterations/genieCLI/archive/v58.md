# genieCLI v58 — `--direct` fragment-rewrite opt-in parity

## Outcome

Shipped in `HEAD` / `origin/main` `6cd9640`:

```text
fix(trino-research): fold review/wrap_retro corrections into HEAD [tlv4 commit-sync]
```

## Behavior

v58 closes the parity gap between MCP and `--direct` paths for fragment-level LLM rewrite:

- `run_mcp_enhancement()` docstring now says max iterations default is `1`, matching runtime.
- All three `--direct` call sites read `GENIE_FRAGMENT_REWRITE` and `GENIE_FRAGMENT_REWRITE_CAP`:
  - no-data path,
  - plan-cost loop,
  - standard loop.
- Those values are passed to the shared decompose candidate helper, mirroring MCP call sites.
- Default safety is preserved: fragment rewrite remains OFF unless `GENIE_FRAGMENT_REWRITE=1`.

## Verification snapshot

From `.tlv4-v58-direct-fragment-optin/artifacts/wrap_retro.producer.md`:

```text
Targeted (test_decompose_then_iterate + test_mcp_research): 102 passed, 0 failed
Full regression: 1614 passed, 11 skipped, 0 failed
```

The local skip count is environment-dependent; the load-bearing claim is 0 failures.

## Open follow-ups

- Narrow broad `except Exception` blocks in env-threading tests in a future cleanup.
- Live-cluster validation for plan-cost and standard-loop paths remains pending before fragment rewrite can be considered for default-on.
