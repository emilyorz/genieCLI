# v37 — trino-optimize pipeline (decompose → optimize → recompose → verify)

> Backfilled 2026-06-13. Fuller detail: STATUS.md + features/trino-research.md design log.
> Built via task-ledger-v4 form-2 (Run A primitives + Run B pipeline).

## Scope
The 5-stage structured optimizer for `/trino-research`: split a query into fragments,
optimize each flagged monster, recompose, and verify row-equivalence as the P0 gate.

## What shipped
- Run A (`b36c359`): deterministic primitives — `detection_scan`, `cost_reader`,
  `rows_equivalent` shim into the MCP research path.
- Run B (`181c747`): `trino_optimize.py` 5-stage pipeline (baseline / decompose /
  optimize / recompose / verify) with SHIP/NO_SHIP/UNVERIFIED/NO_COST_IMPROVEMENT verdicts.
- Live golden-case validated on a real Trino MCP; fixed 2 integration bugs (`__root__`
  rewrite application; recompose UNCERTAIN-vs-ERROR handling).

## Verification
~1043 pass at Run B close. Live golden-case proved an equivalent, cheaper rewrite and
blocked a deliberately-wrong one.

## Deferred / lessons
- Wiring decompose/recompose into write-advisory mode → v40.
- form-2 design-step producers thrash on dense contracts; orchestrator-author + form-2-gate
  is the effective pattern (the optimize *develop* producer delivered real code).
