# v41 — semantic equivalence gate for fragment rewrites

> Backfilled 2026-06-13. Fuller detail: STATUS.md / features design log.

## Scope
Close the v40 adversarial finding: `_column_safe_candidates` guarded column NAMES only, so an
LLM rewrite that keeps names but drops a WHERE / changes a JOIN (R6/R9 are rewrite-tier and
touch predicates) passed the gate. Commits `6d50bea` + close-out `dc9124d`.

## What shipped
`_semantic_safe_candidates` (write_analysis.py:305) runs after the column gate; recompose
status becomes "column AND row-level semantic gates applied"; research.py gains semantic_drift
detection. The row-level correctness hole is closed.

## Verification
~1207 pass at v41 baseline.

## Deferred
tGenie P1–P8 docs (P5 gap) — carried until v47.
