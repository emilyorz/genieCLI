# genieCLI v57 — model-call budget for `/trino-research`

## Outcome

Shipped on `origin/main` via:

- `e11c472` — `fix(trino-research): bound v57 model calls and preserve reports`
- `1581e44` — `chore(trino-research): close v57 task ledger`

## Behavior

- Default decompose seed is evidence-only: no LLM monster ranking and no per-fragment LLM rewrite unless explicitly opted in.
- Fragment rewrite opt-in: `GENIE_FRAGMENT_REWRITE=1`.
- Fragment rewrite cap: `GENIE_FRAGMENT_REWRITE_CAP`.
- Interactive `/trino-research` default max iterations is `1`.
- Standard-loop and plan-cost-loop provider/model failures are recorded as `model_failed` and still produce report objects.

## Verification snapshot

From `.tlv4-v57-model-call-budget/artifacts/wrap_retro.producer.md`:

```text
python -m pytest tests/test_mcp_research.py tests/test_strategy_verify.py tests/test_decompose_then_iterate.py tests/test_step_trace.py tests/test_trino_optimize.py tests/test_critical_path.py -q
301 passed, 1 skipped
```

`py_compile` also passed for the changed production/test files.

## Follow-ups closed by v58

- Stale `run_mcp_enhancement()` docstring default fixed.
- `--direct` received the same `GENIE_FRAGMENT_REWRITE` opt-in wiring as MCP path.
