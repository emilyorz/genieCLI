# v46 — --direct table-metadata parity

> Backfilled 2026-06-13. Fuller detail: STATUS.md. (git: `df11315`)

## Scope
Close the last known MCP/`--direct` asymmetry: the `--direct` path now fetches real table
metadata and feeds the metadata contributor in `pre_execution_diagnosis`.

## What shipped
- F1: shared `_fetch_table_metadata_from_runner(tables, execute_fn)`; MCP adapter
  `_make_mcp_execute_fn` extracts `envelope["rows"]` (the raw return is an envelope dict, not
  a row list — silent-iteration bug avoided). Module-level `_SAFE_IDENT_RE`.
- F2: `_execute_direct_as_dicts` + `_fetch_table_metadata_direct`; `_assemble_direct_directions`
  gains `table_metadata` kwarg and returns a 2-tuple matching the MCP assembler. Duck-type parity.
- F3: `metadata-unavailable` note now conditional on fetch outcome.

## Verification
+28 tests → 1387 pass / 1 skipped (27 former xfails + 1 MCP adapter test). Residuals (office-
cluster backlog): SAFE_IDENT skips quoted idents; table_properties not in all catalogs; a
`_execute_direct_as_dicts` conn-leak on exception; rediag metadata-reuse efficiency gap. Ledger
`.tlv4-v46-direct-metadata/`.
