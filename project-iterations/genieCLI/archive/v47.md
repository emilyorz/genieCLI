# v47 — P1–P8 rewrite-strategy menu codified (P5 gap closed)

> 2026-06-13. Fuller detail: STATUS.md + features design log + phase-reports/v47-p-strategies-PLAN.md.

## Scope
Codify the manager's 8 optimization-rewrite strategies (never in the repo, P5 undefined since
v37) as the explicit, action-tier-tagged **fix menu** the optimizer applies — distinct from
R1–R10 detection. P5 = predicate/partition pushdown (Sam-confirmed).

## What shipped
- T1 `p_strategies.py`: 8 `PStrategy{id,name,trigger,recipe,tier,safety_note}` + tier→action +
  `render_menu()`; `test_p_strategies.py` completeness gate (25 tests).
- T2: `optimize()` prompt injects the menu + tier discipline (was an unconstrained LLM rewrite);
  4 prompt tests. Single rewrite-prompt site, no `--direct` duplicate.
- T3: both SKILL.md files document P1–P8.
- T4: feature design log + STATUS close-out.
Commits PLAN `a374de1`, T1 `53cdbc8`, T2 `d310cfc`, T3 `6763e76`, T4 `f285c89`.

## Verification
1416 pass / 1 skipped (+29 over v46).

## Lessons
Safety tiers: SAFE (P1/P7/P8) auto-apply; TRAP (P2/P5) rewrite only behind row-equivalence;
DANGEROUS (P3/P4/P6) advise-only. Menu = guidance; the v40/v41 semantic gates = hard
enforcement (code manages safety, LLM picks the strategy).
