# TASK-LEDGER

## Basic Info

- Project: genieCLI v20 — report polish based on Sam's feedback
- Repo Folder: project-iterations/genieCLI/
- Iteration: 20
- Owner: Emily (Claude Code)
- Status: done
- Updated: 2026-04-16T15:30+0800
- Focus: Clean up the /trino-research report output — the first real
  user feedback on the generated markdown.

## Feedback from Sam

1. "後面的 original query plan 跟 enhanced query plan 看起來就是一整包的 JSON" —
   the plan section dumps raw JSON (ugly)
2. "Enhanced Result (Sample) <?? 這是甚麼?" — section purpose unclear
3. "Table Structure Suggestions 這應該會進行吧?" — wants to confirm
   this section actually runs
4. "Generated by genieCLI research 應該要加上 Lakehouse Team"

## Changes

| File                                 | Change                                                                                                                                                                                                                                                                                                           |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `genie/skills/mcp_trino/research.py` | `_render_explain`: when stages can't be parsed, render a clean totals summary (CPU / Wall / Memory / Input rows / Output rows) + one-line note instead of dumping raw JSON                                                                                                                                       |
| `genie/skills/mcp_trino/research.py` | Add `sample_note` under both Original Result + Enhanced Result sections: "First 10 rows of the query output, used to visually spot-check that the enhanced SQL preserves the original result set."                                                                                                               |
| `genie/skills/mcp_trino/research.py` | Distinguish "no qualified tables" vs "no issues detected" in Table Structure Suggestions: new `had_qualified_tables` flag on `EnhancementReport`; enhanced loop only counts `catalog.schema.table` refs; when no qualified tables exist, show an actionable hint instead of "No table structure issues detected" |
| `genie/skills/mcp_trino/research.py` | Footer: "Generated by genieCLI mcp_trino research — Lakehouse Team —" (EN + ZH)                                                                                                                                                                                                                                  |
| `tests/test_mcp_research.py`         | Updated no-suggestions test; added unqualified-tables hint test                                                                                                                                                                                                                                                  |

## Verification

- 617 tests pass (up from 616; +1 new)
- Existing `no_suggestions` test updated to set `had_qualified_tables=True` to target that branch explicitly
- New test asserts the unqualified-tables fallback message is rendered

## Retro

- **Worked:** Sam's feedback was specific and actionable. Each item
  mapped to a ~10-line change. Distinguishing "no qualified tables"
  from "no issues" is a meaningful UX improvement — the original
  "No issues detected" was misleading when the real reason was
  missing qualification.
- **Failed:** Didn't notice the raw JSON fallback path during v15-v19.
  A real report would have made this obvious. User testing > code review.
- **Change next:**
  - If MCP servers commonly return plan JSON, could add a JSON plan
    parser that extracts structure (stage tree) for rendering. But
    without a test fixture showing what Sam's server emits, the
    compact totals summary is the safer default.
  - Consider an opt-in `--default-catalog` / `--default-schema` flag
    so users can get Table Structure Suggestions without editing SQL
    to qualify every table name.
