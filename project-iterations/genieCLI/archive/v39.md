# v39 — R10 join-key-computed static rule

> Backfilled 2026-06-13. Fuller detail: STATUS.md / features design log.

## Scope
Sam's v38 CTAS put a computed value on a join key and nothing flagged it. Investigation:
no R1–R9 rule, prompt, or P-strategy ever inspected the JOIN ON predicate for a
function/cast/arithmetic on a key — and tGenie P1–P8 never codified it (P5 undefined).

## What shipped
New rule `rules/r10_join_key_computed.py`: walks `exp.Join.on` → `exp.EQ` → computed-vs-bare
side (medium; low for symmetric normalization; ADVISE, no auto-rewrite). Wired through all 5
registration points, guarded by `test_rule_id_contract.py`. Added to SKILL.md anti-patterns.
Commit `7bf772d`.

## Verification
1075 pass / 11 skipped; 15 new tests.

## Deferred / lessons
v37 decompose into write-advisory → v40. The P5 gap surfaced here was the seed for v47.
