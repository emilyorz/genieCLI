# v45 — preflight state machine + 3 correctness fixes

> Backfilled 2026-06-13. Fuller detail: STATUS.md.

## Scope
Decision-layer refactor for `/trino-research` + 3 fixes, dual-path symmetry by construction.

## What shipped
- S1: `PreflightRoute` enum (6 values) + `PreflightDecision` + `build_preflight_decision`
  shared by both paths; fixes the `max_iterations==0` guard gap on `--direct`.
- S2: `detect_no_data_reason` narrowed from bare substring to tier-1 exact tokens + tier-2
  anchored container-subject regex (10 non-container subjects now surface as real failures).
- S3: honest `metadata-unavailable` direction on `--direct`.
- S4: truncation signal in `format_directions_for_prompt`.

## Verification
+84 tests → 1359 pass / 1 skipped (66 new + 18 parametrized; 21 xfail→pass). Ledger
`.tlv4-v45-preflight-sm/`.
