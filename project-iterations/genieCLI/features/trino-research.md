# Feature: trino-research

> The `/trino-research` slash command — iterative SQL optimizer that uses MCP-Trino (or direct driver as fallback) to test candidate rewrites against a real Trino server.

## Current capability

- Invoked from `genie chat` as `/trino-research [--file <path>] [--metric <m>] [--iterations <n>] [--runs <n>] [--safe-limit <n>] [--query-timeout <sec>] [--direct] [--help|-h]`.
- Default routing: when `[mcp.trino].enabled = true`, calls into MCP-based execution (`run_trino_research_via_mcp`); `--direct` forces the legacy direct-driver path.
- Pre-launch UX (v22): renders a Plan card showing sql / metric / iterations / verify-runs / server / safety-limit / timeout. v24 added 5-line SQL syntax highlight preview at the top of the card.
- Loop UX (v22-v23): per-iteration status block (`KEPT / WORSE / REVERT / FAIL / SKIP` color-coded) with elapsed time; SQL diff between candidates; live spinner during AI thinking; per-run progress label for verify loops.
- Post-loop UX (v22 + v27): final summary card with baseline vs best bar chart and improvement arrow. v27 dropped the full Optimized-SQL terminal dump — the report file is the source of truth for SQL bodies; terminal stays scannable.
- Report UX (v27): markdown report is iteration-centric — Summary table → compact iteration history table → per-iteration sections (full hypothesis + verdict + mini diff scoped to that round) → single Best SQL block at end. No more triple-SQL duplication. Reports save to `./report/<timestamp>.md` (auto-created) instead of cwd.
- Help (v23 inline + v24 routing): `/trino-research --help` and `/help trino-research` both show the same help card with usage / flags / examples.
- Safety (v19): preflight gate enforces read-only whitelist + EXPLAIN-based size estimation + row cap; `--safe-limit` opt-in wraps query in `LIMIT n`; default query timeout 300s.
- Long-query gate (v28 T5): after baseline measurement, if `baseline wall_time > --long-query-threshold` (default 60s) and `--long-query` is not set, the run aborts with a predicted-worst-case message. `--max-fallbacks` configures the K-retry cap for the final L3 row-equivalence verify (default 3). On the MCP path, `SET SESSION query_max_run_time = '<1.2×baseline>ms'` is emitted best-effort before iteration so Trino-side kills overshooting candidates (direct-path session-property support deferred to T4).
- Output sinks (v18+): HumanSink (Rich, colored, no emojis) by default; MachineSink (JSON) when piped.

## Design log (append-only)

- **v17:** `/trino-research` hard-requires MCP, no silent fallback. `--direct` is opt-in. Reason: silent fallback hid configuration errors; users were debugging "why is MCP not used" without knowing the path was silently swapping to direct. Alternatives considered: warn-and-fallback — rejected, warnings get lost in normal output.
- **v18:** Trino best practices moved from hardcoded Python into `mcp_trino/SKILL.md` body, injected via `parse_skill_md_body() + SkillRegistry.get_instructions(group)`. Reason: tuning the AI's optimization knowledge required code changes; SKILL.md body lets us iterate via markdown.
- **v19:** Preflight gate before any execution. Reason: v18 could OOM on huge result sets, AI could (in principle) emit DML, default 30s timeout was too short for real queries. Triple-defense: read-only whitelist + EXPLAIN size cap + opt-in `--safe-limit` wrapper.
- **v22:** UX overhaul prioritized "make every loop phase legible" over "richer output". Reason: Sam said "希望是有感的提升" — opaque progress() lines were the felt pain. Helpers stay pure (output + kwargs in, nothing else) so they're trivially unit-testable.
- **v24:** MCP status banner runs synchronously at startup with 3s timeout. Reason: simplest path to first-pass visibility; symmetric with `genie doctor`'s probe so users learn one failure mode. Alternatives considered: async background probe — rejected for v24 because Rich console redraw during prompt readline was deemed fragile (revisited in v25).
- **v25:** `_execute_via_mcp` now handles **bare-list response shape** (`[{...}, ...]`) in addition to wrapped (`{"rows": [...]}`). Reason: mcp-trino's actual contract returns a bare JSON list; the previous `data.get("rows", []) if isinstance(data, dict)` silently dropped to empty rows on every query. Side-effect bug fixed at the same time: optimizer's `capture_rows=True` equivalence check was always comparing `[]` vs `[]` → silently treating different SQL outputs as equivalent. Alternatives considered: require all MCP servers to wrap responses — rejected, no standard exists; servers in the wild use both shapes.
- **v25:** `_measure_mcp` now backfills server-side metrics from `EXPLAIN ANALYZE` text when the server's response carries no structured `metrics` dict. Reason: mcp-trino executes the SQL but returns only rows, so `cpu_time_ms`, `peak_memory_bytes`, `processed_rows` were always 0 and the optimizer couldn't rank candidates by anything except wall-clock elapsed. The existing `_parse_explain_stages` already knew how to extract metrics from Trino's text output; reuse it. Cost: 2N queries instead of N when backfill triggers. Alternatives considered: require MCP servers to populate metrics dict — rejected, can't change contract for servers we don't own; transparent backfill keeps both shapes working.
- **v27:** `/trino-research` output UX trio — terminal stops printing the full Optimized SQL (keeps Summary + iteration lines + `Report saved:` pointer), report is rewritten iteration-centric (one Best SQL at the end, per-iteration section with full hypothesis + mini diff + verdict), and report path moves to `./report/<name>.md` with auto-`mkdir`. Reason: Sam's feedback "上次我測試時 有 diff 雖然不錯, 但整個我覺得變得有點混亂" — three pain points in one iteration: 70-line SQL dump on terminal, triplicate SQL in report (Original + Optimized + diff), cwd pollution. Alternatives considered: (a) keep Original as appendix — rejected, doesn't address "三份看三次"; (b) minimal patch (just un-truncate hypothesis) — rejected, rearranges deck chairs; (c) global `~/.genie/reports/` — rejected, Sam explicitly asked for cwd `./report/`. To enable per-iteration diffs the history entries now carry `base_sql` + `candidate_sql`; hypothesis storage no longer caps at 80 chars (only the terminal progress line truncates for display).
- **v28 T2+T5 (upfront cost gate + plan_cost helper):** Baseline-time × N-iteration × N-run geometric blow-up made `/trino-research` unusable for queries where the baseline itself runs 1h+ (Sam's direct feedback via Telegram msg 818: "那這類的 query 在進行auto research 不就會非常的浪費時間?"). **T2:** Added `plan_cost(sql, explain_runner) -> (rows, bytes, raw_plan_json)` to `mcp_trino/preflight.py` (reusing existing `estimate_from_explain`); this gives the v28 loop a way to rank candidates without executing them. PLAN-deviation: helper landed in `preflight.py` not `trino_query/research.py` — rationale: shared location lets both MCP + direct paths consume it without circular-import churn, and it's thematically co-located with `estimate_from_explain`. **T5:** Three-part gate. (a) Upfront wall-time gate — after baseline measurement, if `baseline_s > --long-query-threshold` (default 60s) and `--long-query` is not set, abort with predicted-total-time message using `baseline × (1 + iter×1.2 + 1 + max_fallbacks)` math (matches PLAN's 11× worst case for 5-iter+3-fallback defaults). (b) Per-candidate Trino-side kill — `SET SESSION query_max_run_time = '<ceil(1.2 × baseline_wall_ms)>ms'` emitted best-effort via `_execute_via_mcp` before the iteration loop; session-property persistence across MCP tool calls is untested in production (flagged as live-env open question for T7). (c) New flags `--long-query`, `--long-query-threshold <s>`, `--max-fallbacks <n>` threaded through `run_trino_research_via_mcp` / `run_mcp_enhancement` / `run_trino_research` / `_run_optimization_loop`. Correctness guard (L1 structural signature + L3 row-equivalence + K-retry) is the Option 1 design Sam ack'd via Telegram msg 824/825, but L1 implementation (T3/T4) is deferred — see Limits v28. Helpers are pure and unit-tested (16 new tests under `TestPlanCost` / `TestLongQueryGate` / `TestMakeQueryMaxRunTimeSql`; 647 total pass). Alternatives considered during PLAN: L3-only + K-retry (Option 2) — rejected because a structurally-wrong LLM candidate with lower plan cost would rank first, burning 1h on each L3 fallback; L1 is the cheap pre-filter that absorbs that blast radius. L1+L2+L3 (Option 3) — rejected, L2 sample-row verify requires Trino-aware AST substitution which is 2-3× L1's engineering cost for marginal gain over L1 + K-retry. No correctness check — rejected, regression from the existing `_results_equivalent` gate. Direct-path session-property emission deferred to T4 (requires `connection.py` `session_properties` kwarg plumbing).

## Limits (append-only)

- **v17:** Direct-driver path retained but de-prioritized — only invoked under explicit `--direct` flag. Reason: maintaining two execution paths is testing burden. Revisit when: MCP becomes 100% reliable in production AND zero users need direct mode.
- **v18:** SKILL.md body limit not enforced — currently 6KB after v24 expansion. Reason: no measured token-budget pressure yet. Revisit when: SKILL.md body exceeds 12KB OR Trino enhancement runs show truncation.
- **v22:** SQL diff capped at 20 lines with truncation note. Reason: terminal real estate; longer diffs become unscrollable. Revisit if users ask for `--full-diff` flag.
- **v24:** MCP banner probe is synchronous, blocks chat startup for ~3000 ms when MCP is unreachable. Reason: see v24 design log. Revisit: **v26 in flight** — async refactor (deferred from v25 by metric-pipeline hotfix).
- **v26:** MCP-contract testing discipline — when fixing a bug whose root cause is an MCP-server response-shape assumption, the unit test must exercise the **actual server's response shape**, not the convenient default. Concrete failure mode: v25's `13dbfab` shipped a "fix" whose unit test passed (mock returned dict-shaped response), but production failed (real mcp-trino returns bare-list). Sam's office re-run revealed the upstream bug. Mitigation: when adding a test that touches `_execute_via_mcp` or `_fetch_explain_analyze`, prefer `json.dumps([{...}, ...])` (bare-list) over `json.dumps({"rows": [...], "metrics": {...}})` unless specifically testing the wrapped-shape path. Update this note if a third response shape variant appears.
- **v28 T5 (L2 sampled row verify deferred):** Option 3 from PLAN (L1 + L2 + L3) was rejected. L2 would substitute base tables with `TABLESAMPLE BERNOULLI(0.1)` versions and compare rows on the sample before committing to the full L3 verify. Reason rejected: table substitution across CTEs, subqueries, and joins requires a Trino-aware AST parser we don't have (`sqlparse` is shallow), and `TABLESAMPLE` doesn't compose cleanly with aggregations (sum/count change under sampling). Revisit when: L1 + K-retry (Option 1) shows a real "structural-match-but-data-different" failure class in production that K-retry doesn't surface quickly enough — i.e., multiple consecutive 1h-fallback burns on the same run. Until then, K-retry is the absorption layer.
- **v28 T5 (SET SESSION persistence unverified on MCP):** The `SET SESSION query_max_run_time` is emitted via `_execute_via_mcp` as a one-shot tool call before iteration. Whether mcp-trino persists session state across subsequent tool calls is not tested in this iteration (localhost:8811 down at T1 probe time). Behaviour is best-effort — emit wrapped in try/except, failure logs to `output.progress` and iteration proceeds. If the SET doesn't persist, candidate queries exceeding 1.2× baseline will still burn wall-time but correctness is unaffected (the L3 row-equivalence check is the real gate). Revisit when: T7 smoke or first real production run reveals the actual mcp-trino session-scope semantics. May be resolved by: switching to a per-query session-property header if mcp-trino exposes one, or by forcing `--direct` path which threads session_properties via the Trino Python client.
- **v28 T5 (direct-path session property deferred):** On `--direct` runs the gate still fires, but `SET SESSION` is not emitted — `connection.py` doesn't yet accept a `session_properties` kwarg on `get_active_profile().connect()`. Deferred to T4 where the loop rewrite is already touching the per-candidate execution path. Revisit when: T4 lands or a direct-mode user reports a runaway candidate.

## Open questions

- Does the AI's hypothesis extraction (first non-code line) need a more structured prompt? Currently brittle — sometimes the AI buries the hypothesis in code comments.
- Should the report's Performance Comparison table also gain ASCII bars (currently only the live summary card has them)?
- Is there a configurable "give up early" rule for the iteration loop? Right now it always runs N iterations even if rounds 1-2 already converged.

## Iteration touchpoints

- **v17:** MCP hard-requirement; `--direct` opt-in; dynamic tool name + SQL parameter discovery.
- **v18:** SKILL.md body injection; Trino best practices moved to markdown.
- **v19:** Preflight gate (read-only / size estimate / row cap / safe-limit / 300s timeout default).
- **v20:** Report polish — compact EXPLAIN summary, sample-note, table-suggestions messaging, Lakehouse footer.
- **v21:** Bug fixes — paste-mode leak, null-tool guard, EXPLAIN us/ns parser.
- **v22:** UX sprint pt.1 — SQL diff, iteration status block, plan card, summary card.
- **v23:** UX sprint pt.2 — live spinner, per-run progress, inline `--help`.
- **v24:** UX sprint pt.3 — SQL preview in plan card, `/help trino-research` routing, MCP banner, SKILL.md expert content expansion.
- **v25:** Metric pipeline fix — `_execute_via_mcp` handles bare-list response shape (was silently dropping all rows on mcp-trino), `_measure_mcp` backfills server metrics from `EXPLAIN ANALYZE` text via existing `_parse_explain_stages`. Net effect: optimizer can now rank candidates by real CPU/Memory/Input rows on mcp-trino, not just wall-clock. Side-effect bug also fixed: row-equivalence check was a no-op. Validated end-to-end on Sam's Trino 467 + localhost:8811. 641 tests pass (+2 new).
- **v26 T2:** Added MCP-contract testing discipline note to Limits section. No code change; process discipline. Closes v25 retro's "End-to-end smoke discipline" promote.
- **v26 T5:** Closed out v15 R3 (tests landed in v25 commit b3f7566 — `test_execute_via_mcp_handles_bare_list_response` + `test_measure_mcp_backfills_metrics_from_explain_analyze`; 641 pass) and v10 T9 (live MCP verify against Sam's Trino 467 + localhost:8811 — `_measure_mcp` now produces real CPU≈35us, Peak Memory=132B, Input=1 row on `SELECT 1`; full optimizer runs on real queries show non-zero metrics). Both parks deleted from STATUS.md / CURRENT.md. The metric-pipeline saga that started in v10/v15 is formally done.
- **v27 T1-T4:** `/trino-research` output UX overhaul — three edits to `genie/skills/trino_query/research.py`: (1) drop the full-Optimized-SQL terminal loop (kept only a compact "no improvement" fallback line); (2) rewrite `_generate_report` iteration-centric with a new `_iteration_diff` helper using stdlib `difflib`, no new dep; (3) report path → `./report/<name>.md` with `mkdir(parents=True, exist_ok=True)`. Supporting data change: history entries now carry `base_sql` + `candidate_sql` so each iteration section can render its own mini diff; hypothesis un-truncated (was `[:80]` at extraction + `[:60]` at report — now full string, only terminal progress line display-truncates). T5 (real-run verification) dropped at Sam's request (Telegram msg 813); the feature will be exercised on first natural use after merge.
- **v28:** (partial landing — T2 + T5 only; T1/T3/T4/T7 carried over). Added `plan_cost()` helper to `mcp_trino/preflight.py` (reused by the planned v28 loop rewrite) and the upfront long-query cost gate + per-candidate wall-clock kill primitives. **Files touched:** `genie/skills/mcp_trino/preflight.py` (+123 lines — `plan_cost`, `check_long_query_gate`, `make_query_max_run_time_sql`, `LongQueryAbort`, constants), `genie/chat.py` (+3 flags + help text), `genie/skills/mcp_trino/research.py` (signatures + post-baseline gate + best-effort `SET SESSION` emit + `LongQueryAbort` catch at entry-point), `genie/skills/trino_query/research.py` (same signatures + gate + `{"status": "aborted"}` return shape), `tests/test_mcp_preflight.py` (+16 unit tests across `TestPlanCost` / `TestLongQueryGate` / `TestMakeQueryMaxRunTimeSql`). **Test state:** 647 pass, 10 skip (0 regressions from the 641 baseline). **Deferred within v28:** T1 (Trino EXPLAIN JSON probe) blocked — localhost:8811 HTTP 000 at probe time (MCP-trino server not running); T3 (`_explain_plan_signature` + `_structural_equivalent`) depends on T1 fixtures; T4 (loop rewrite to plan-cost ranking + K-retry) depends on T3 under Option 1; T7 (end-to-end smoke) depends on the same Trino env as T1. Direct-path session-property emission also folded into T4's scope. The correctness-tier design Sam ack'd (Option 1 — L1 structural + L3 row-equivalence + K=3 retry) is locked; the deferred tasks carry the L1 work and will resume once the Trino env is reachable.
